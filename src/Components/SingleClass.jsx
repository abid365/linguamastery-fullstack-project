import React from "react";

const SingleClass = (props) => {
  console.log(props.singleclass);
  return (
    <div>
      <div className="">{/* <h1 className="font-bold">{language}</h1> */}</div>
    </div>
  );
};

export default SingleClass;
